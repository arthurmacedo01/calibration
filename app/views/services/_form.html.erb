<%= form_with(model: service) do |form| %>
  <% if service.errors.any? %>
    <div style="color: red">
      <ul>
        <% service.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group mb-2">
    <%= form.label :code, style: "display: block" %>
    <%= form.text_field :code, class: "form-control", required: true %>
  </div>

  <div class="form-group mb-2">
    <%= form.label :description, style: "display: block" %>
    <%= form.text_area :description, class: "form-control" %>
  </div>

  <div class="form-group mb-2">
    <%= form.label :section_id, style: "display: block" %>
    <%= form.select(:section_id, @sections.map{ |section| [section.name,section.id]}, {}, class: "form-select") %>
  </div>

  <div class="form-group mb-2">
    <%= form.submit class:"btn btn-primary mb-2" %>
  </div>
<% end %>
